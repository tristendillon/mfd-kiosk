# Nix build outputs
/result
/result-*
/os/result
/os/result-*

# Copied-out ISO artifacts (`nix run ./os#iso` writes ./dist; stray *.iso copies)
/dist/
/os/dist/
*.iso

# direnv
/.direnv/

# scratch spike screenshots (not part of the repo)
/firefox.png
/webkit.png
/stuck.png

nixls
