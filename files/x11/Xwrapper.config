# Managed by mfd-kiosk installer.
# Allow the user logged in on the console VT (the autologin kiosk user) to
# start the X server via startx. `console` is enough because the kiosk autologs
# in on tty1. If a no-KMS device fails to start X, also set:
#   needs_root_rights=yes
allowed_users=console
